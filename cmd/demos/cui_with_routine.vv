times := import("times")
fmt := import("fmt")
cui := import("cui")

app := cui.new()

writeRoutine := func() {
    for i := 0; i < 100; i++ {
        app.write("Hello from writeRoutine: " + i+"\n")
        times.sleep(500*times.millisecond)
    }
}

go(writeRoutine)

app.run()